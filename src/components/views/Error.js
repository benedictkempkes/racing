import React from 'react';

const Error = () => (
    <div style={{display: 'flex', justifyContent: 'center', height: '92vh', alignItems: 'center', textAlign: 'center', padding: '5%'}}>
        <div>
            Es tut mir leid, aber ich hoste das Backend auf einer Kartoffel. Deswegen kann es dazu kommen, dass die Daten nicht geladen werden. Dann die Seite einfach neuladen.
        </div>
    </div>
)

export default Error